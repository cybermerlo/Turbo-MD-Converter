# File con nome riservato Windows (Git non può tracciarlo)
/nul

# Ambiente e segreti
.env
.env.local
*.local

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
venv/
.venv/
env/

# Build e distribuzione
build/
dist/
*.egg-info/
*.egg

# Output e cache (cartelle generate dall'utente, non il modulo Python source)
output/*
!output/*.py
*.log

# File generati dall'installer
installer/output/
# version.iss è rigenerato da build_installer.py (contiene path e versione locali)
installer/version.iss

# Stato di build incrementale, riscritto a ogni build (non è sorgente)
build_info.json
